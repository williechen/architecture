[package]
name = "sql_derives"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]

chrono = { version = "0.4", features = ["serde"] }

proc-macro2 = "1.0"

quote = "1.0"

syn = { version = "2.0", features = ["full"] }
